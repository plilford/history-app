node_modules/
dist/
tsconfig.tsbuildinfo
.env
.env.local
*.local

# Cloudflare Pages Functions local secrets (ANTHROPIC_API_KEY etc.)
.dev.vars
.wrangler/
# Local-only wrangler config (prod build is configured via the CF dashboard;
# local `npm run pages:dev` passes the needed flags explicitly).
wrangler.toml

# editor / OS
.DS_Store
.vscode/
.idea/

# python (importer)
__pycache__/
*.pyc
.venv/
venv/

# Supabase CLI local state
supabase/.branches/
supabase/.temp/

# Claude Code per-user settings
.claude/settings.local.json

# Bubblewrap / Android — keep config, never commit binaries or signing key
android/android.keystore
android/*.keystore
android/*.jks
android/*.keystore.bak
android/*.apk
android/*.apk.idsig
android/*.aab
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
android/*.iml
android/keystore.json

# Curation script dry-run artifacts (regenerable)
tools/v2/curation/proposed_*.json
tools/v2/curation/c6_purge_tuples.txt

# Local DB backup snapshots from tools/backup_hosted.py
backups/

.dev.vars*
!.dev.vars.example
!.env.example
